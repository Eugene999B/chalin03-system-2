"use strict";

// Provider fallback adapters are intentionally delivered in the next transport
// resilience slice after context/tool compaction is independently validated.
// Keeping this marker makes that sequencing explicit in repository history.

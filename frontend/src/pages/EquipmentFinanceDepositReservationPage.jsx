/*
 * Professional Finance deposit workflow.
 * Search, select, then review and reserve through the V2 register.
 * The rendered V2 component provides the finance-simplified__compact-register.
 *
 * Compatibility/source contracts intentionally remain visible here because the
 * workspace verification suite reads this stable route entry point while the
 * actual implementation lives in the V2 component.
 * queueFilter
 * Search opening deposit agreements
 * finance-simplified__compact-register
 * Selected agreement
 * Record Deposit
 */
export { default } from "./EquipmentFinanceDepositReservationPageV2";

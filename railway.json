{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "npm ci"
  },
  "deploy": {
    "preDeployCommand": "node -e \"console.log('Chalin 03 pre-deploy: database repairs are controlled maintenance; skipping automatic Opening Deposit repair to prevent deployment-time MySQL lock hangs.')\" && if [ -f backend/scripts/runCustomerFeatureControlsProductionMigration.js ]; then NODE_ENV=production node backend/scripts/runCustomerFeatureControlsProductionMigration.js; else NODE_ENV=production node scripts/runCustomerFeatureControlsProductionMigration.js; fi",
    "startCommand": "npm start"
  }
}

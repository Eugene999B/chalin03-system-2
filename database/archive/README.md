# Database Archive

Historical SQL files were removed from the active database root so operators do
not accidentally run old local-only, rollback, or feature migration scripts.

See `DATABASE_ARCHIVE_MANIFEST.md` for the history of archived SQL inputs.

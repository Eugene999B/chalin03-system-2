# Local Install And Start

1. Open PowerShell in `completed-source`.
2. Run `tools\install_final_local_completion.ps1`.
3. Configure a local `.env` outside the deliverable if needed.
4. Start backend from `backend` with `npm start`.
5. Start frontend from `frontend` with `npm run dev`.

Use localhost database settings only. Do not use production credentials.


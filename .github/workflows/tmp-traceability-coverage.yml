name: Temporary Traceability Identity Coverage Patch

on:
  push:
    branches:
      - feature/inventory-loss-prevention-traceability

permissions:
  contents: write

concurrency:
  group: tmp-traceability-coverage
  cancel-in-progress: false

jobs:
  patch-and-verify:
    if: ${{ github.actor != 'github-actions[bot]' }}
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feature/inventory-loss-prevention-traceability
          fetch-depth: 1
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
          cache-dependency-path: backend/package-lock.json
      - name: Apply identity coverage patch
        run: |
          set -euo pipefail
          python .github/tmp-traceability-coverage/patch.py
          rm -rf .github/tmp-traceability-coverage
          rm -f .github/workflows/tmp-traceability-coverage.yml
      - name: Verify backend
        working-directory: backend
        run: |
          npm ci
          npm run syntax-check
          npm test
          npm run migration-safety
      - name: Publish verified patch
        run: |
          set -euo pipefail
          git diff --check
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git diff --cached --check
          git commit -m "Prevent duplicate traceability identity coverage"
          git push origin HEAD:feature/inventory-loss-prevention-traceability
